[package]
name = "aura_udot_v2"
version = "0.1.0"
edition = "2021"
description = "Deterministic Aura UDOT v2 derivation and validation with explicit legacy v1 compatibility"

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"

[dev-dependencies]
serde_json = "1"
