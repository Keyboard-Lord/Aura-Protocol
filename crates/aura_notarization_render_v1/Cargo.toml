[package]
name = "aura_notarization_render_v1"
version = "0.1.0"
edition = "2021"
description = "Reusable notarization presentation renderer for the frozen Aura v1 summary surface"

[dependencies]
aura_notarization_export_v1 = { path = "../aura_notarization_export_v1" }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
