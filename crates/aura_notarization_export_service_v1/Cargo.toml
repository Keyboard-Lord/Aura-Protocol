[package]
name = "aura_notarization_export_service_v1"
version = "0.1.0"
edition = "2021"
description = "High-level downstream receipt bundle export facade for the frozen Aura notarization summary surface"

[dependencies]
aura_notarization_render_v1 = { path = "../aura_notarization_render_v1" }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
