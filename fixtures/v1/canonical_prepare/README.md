# Aura v1 Canonical Prepare Fixture

Classification: `VALIDATION`

This directory is the locked validation corpus for canonical proof preparation.

It fixes the upstream byte inputs consumed by the L3 pipeline.
